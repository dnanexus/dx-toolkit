process.container = 'quay.io/nextflow/bash'
profiles {
    debug { process.beforeScript = 'echo $HOSTNAME' }

    first {
        params.input = "first_profile"
    }
    second {
        params.input = "second_profile"
    }

}
